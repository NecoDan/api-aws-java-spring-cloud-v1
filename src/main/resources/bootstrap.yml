spring:
  application:
    name: spring-boot-localstack
---
spring:
  config:
    activate:
      on-profile: prod
aws:
  paramstore:
    enabled: true
---
spring:
  config:
    activate:
      on-profile: localstack
  cache:
    type: redis
  redis:
    database: 0
    host: localhost
    port: 6379
    connect-timeout: 6000
    password: Redis2024!
aws:
  paramstore:
    enabled: true
    endpoint: http://localhost:4566
  secretsmanager:
    enabled: false
    endpoint: http://localhost:4566
